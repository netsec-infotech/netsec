// GSAP Plugins
//
// Note:
// - `ScrollTrigger` ships with the `gsap` npm package.
// - `SplitText` and `ScrollSmoother` are Club GSAP (paid) plugins.
//   If you have them, you can wire them back in; otherwise they remain undefined.

import ScrollTrigger from "gsap/ScrollTrigger";

export { ScrollTrigger };
export const SplitText = undefined;
export const ScrollSmoother = undefined;
